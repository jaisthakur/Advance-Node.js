const {holdSeats} = require('./api')
test('should be defined', () => {
    expect.anything();
})

// test('there is a new flavor idea', () => {
//     expect(fetchNewFlavorIdea()).toBeDefined();
//   });